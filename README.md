ToDo-List
Описание
ToDo-List — консольное приложение на Go для управления списком задач.
Приложение позволяет добавлять задачи, просматривать список, отмечать задачи выполненными, удалять их, а также вести журнал действий пользователя.
Для запуска программы нужно запустить main.go файл  

Поддерживаются следующие команды:
  help - показать список доступных команд // help (пример команды)
  add {заголовок} {описание} - добавить задачу // book (первое слово = название задачи) read the book Flowers for Algernon (пример команды) 
  list - показать список задач // list (пример команды)
  del {заголовок} - удалить задачу // delete book (пример команды)
  done {заголовок} - отметить задачу выполненной // done book (пример команды)
  events - показать историю действий //events (пример команды)
  exit - выход из программы // exit (пример команды)
Roadmap (в разработке)

В ближайшее время планируется расширение проекта до полноценного backend-сервиса:
✅ CLI-версия (реализовано)
🔄 HTTP API (REST) на основе текущей бизнес-логики
🔄 PostgreSQL для хранения задач
🔄 Docker / docker-compose для контейнеризации
🔄 Telegram Bot API — управление задачами через Telegram
